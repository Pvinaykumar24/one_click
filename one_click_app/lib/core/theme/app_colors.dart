import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF0a0f14);

  // Neo-brutalism theme - thick borders, hard shadows on dark base.

  static const Color surface = Color(0xFF121b29);         // cards/panels
  static const Color surfaceLight = Color(0xFF1a2535);    // elevated cards

  // Primary accent — electric blue
  static const Color primary = Color(0xFF3B82F6);         // blue-500
  static const Color primaryDark = Color(0xFF2563EB);     // blue-600

  // Neon accents
  static const Color neonCyan = Color(0xFF00F5FF);
  static const Color neonPink = Color(0xFFFF2D78);
  static const Color neonYellow = Color(0xFFFFD600);
  static const Color neonGreen = Color(0xFF39FF14);

  // Semantic colours
  static const Color success = Color(0xFF22C55E);         // green-500
  static const Color warning = Color(0xFFF59E0B);         // amber-500
  static const Color error   = Color(0xFFEF4444);         // red-500
  static const Color info    = Color(0xFF60A5FA);          // blue-400

  // Text
  static const Color textPrimary   = Color(0xFFE2E8F0);  // slate-200
  static const Color textSecondary = Color(0xFF94A3B8);  // slate-400
  static const Color textDisabled  = Color(0xFF475569);  // slate-600

  // Borders
  static const Color border      = Color(0xFF1E293B);     // slate-800
  static const Color borderLight = Color(0xFF334155);     // slate-700

  AppColors._();
}
